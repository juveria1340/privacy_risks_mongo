apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: dissertation
  labels:
    app: mongodb
    tier: database
spec:
  selector:
    app: mongodb
  ports:
  - port: 27017
    targetPort: 27017
    name: mongo
  type: ClusterIP
